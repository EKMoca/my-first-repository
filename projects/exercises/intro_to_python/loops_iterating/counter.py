# Chapter: Collections and Iteration
# sub-chapter, Loops and Iterating

counter = 2
while counter <= 100:
    print(counter) # iterates over the block for all integer 
    counter += 8   # values between 1 and 10, inclusive. 
# block must modify the object counter, or else Infinite Loop.
# each block run is called an 'iteration'.